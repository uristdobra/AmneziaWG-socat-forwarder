# AmneziaWG socat forwarder 🚀

Скрипт для автоматической установки и управления каскадным VPN на базе **AmneziaWG**.  
RU-сервер принимает UDP-трафик от клиента и прозрачно пересылает его на зарубежный сервер AmneziaWG через `socat`.

---

## ✨ Возможности

- ✅ Автоматическая установка пакетов (`curl`, `wget`, `socat`)
- ✅ Создание `systemd`-службы `wg-forward.service`
- ✅ Перенаправление UDP-порта с RU-сервера на зарубежный AmneziaWG-сервер
- ✅ Удобное числовое меню управления
- ✅ Глобальная команда `menu` для управления
- ✅ Пошаговые сообщения о выполнении операций
- ✅ Автозапуск при загрузке системы

---

## 📌 Требования

- **Debian / Ubuntu** сервер (Россия)
- **Root-доступ** (при первой установке)
- Настроенный зарубежный сервер с **AmneziaWG** (self-hosted)
- Из конфигурации AmneziaWG должны быть известны:
  - **IP-адрес** сервера (из Endpoint)
  - **UDP-порт** (из Endpoint)

---

## ⚡ Быстрый старт

### 🎯 Установка одной командой (рекомендуется)

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/uristdobra/AmneziaWG-socat-forwarder/main/install.sh)
```

**Готово!** Скрипт автоматически:
- Проверит права root (попросит пароль если нужно)
- Установит все необходимые пакеты
- Спросит IP и порт зарубежного сервера
- Создаст и запустит службу
- Установит команду `menu` в `/usr/local/bin/`

### После установки

Используйте команду для управления:

```bash
menu
```

**Всё просто!** Пишите `menu` — скрипт сам запросит `sudo` если понадобится. 🚀

---

## 📋 Меню управления

Откройте меню командой:

```bash
menu
```

Вы увидите:

```
╔════════════════════ AmneziaWG socat FORWARDER ════════════════════╗
║                                                                  ║
║  1 🔧 Установить/переустановить форвардер                   ║
║  2 ▶️  Запустить службу                                    ║
║  3 ⏹️  Остановить службу                                   ║
║  4 🔄 Перезапустить службу                                ║
║  5 📊 Показать статус                                     ║
║  6 🗑️  Удалить службу                                     ║
║  0 👋 Выход                                              ║
║                                                                  ║
╚════════════════════════════════════════════════════════════════════╝

Выбор (0-6): 
```

**Просто введите цифру и нажмите Enter!**

| Номер | Действие |
|-------|----------|
| **1** | Установить или переустановить форвардер |
| **2** | Запустить службу |
| **3** | Остановить службу |
| **4** | Перезапустить службу |
| **5** | Показать статус и логи |
| **6** | Удалить форвардер полностью |
| **0** | Выход из меню |

---

## 🔧 Как это работает

### Первый запуск

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/uristdobra/AmneziaWG-socat-forwarder/main/install.sh)
```

Скрипт спросит:
```
IP зарубежного сервера: 37.1.204.148
UDP-порт сервера: 40773
```

Затем установит всё и создаст команду `menu`.

### Ежедневное управление

```bash
menu
```

Выберите нужный пункт (0-6). Скрипт сам запросит права если понадобятся.

---

## 🎯 Как работает форвардер

```
[AmneziaWG Client] 
    ↓ UDP запрос
[RU-сервер: порт X] (socat)
    ↓ пересылка
[Зарубежный AmneziaWG-сервер: тот же порт X]
```

На RU-сервере запускается `socat`, который:
- Слушает локальный UDP-порт
- Пересылает весь трафик на зарубежный сервер
- Работает в фоновом режиме как `systemd`-служба
- Автоматически перезапускается при сбое

---

## 📝 Примеры использования

### Пример 1: Первая установка

```bash
# Скачиваем и запускаем (установка начнётся сразу)
bash <(curl -fsSL https://raw.githubusercontent.com/uristdobra/AmneziaWG-socat-forwarder/main/install.sh)

# Вводим IP и порт (из конфига AmneziaWG)
IP зарубежного сервера: 37.1.204.148
UDP-порт сервера: 40773

# Ждём завершения установки
# ✅ VPN-форвардер успешно установлен!
```

### Пример 2: Проверка статуса

```bash
menu
# Выбираем: 5
# Видим статус службы и логи
```

### Пример 3: Остановка и запуск

```bash
menu
# Выбираем: 3 (остановить)
# Выбираем: 2 (запустить)
```

---

## 🏗️ Что делает скрипт при установке

1. **Проверяет права** (требуется root)
2. **Обновляет пакеты** (`apt update && apt upgrade`)
3. **Устанавливает утилиты:** `curl`, `wget`, `socat`
4. **Спрашивает параметры:** IP и порт зарубежного сервера
5. **Создаёт файл** `/etc/systemd/system/wg-forward.service`
6. **Генерирует команду** socat с вашими параметрами:
   ```
   socat -T15 udp-recvfrom:<PORT>,reuseaddr,fork udp-sendto:<IP>:<PORT>
   ```
7. **Запускает службу** и включает автозапуск
8. **Копирует скрипт** в `/opt/amneziawg-forwarder/menu.sh`
9. **Создаёт symlink** `/usr/local/bin/menu` → `/opt/amneziawg-forwarder/menu.sh`

---

## 📂 Расположение файлов

| Файл | Путь |
|------|------|
| **Служба** | `/etc/systemd/system/wg-forward.service` |
| **Скрипт меню** | `/opt/amneziawg-forwarder/menu.sh` |
| **Команда меню** | `/usr/local/bin/menu` (symlink) |

---

## 🐛 Решение проблем

### Служба не запускается

```bash
menu
# Выбираем: 5 (показать статус)
# Видим ошибку
```

Проверьте вручную:

```bash
systemctl status wg-forward.service -l
```

### Порт уже занят

```bash
netstat -tlnup | grep <ПОРТ>
```

### Нет соединения

- Проверьте, что зарубежный сервер доступен по IP
- Убедитесь, что UDP-порт открыт в firewall
- Проверьте конфиг AmneziaWG на клиенте

---

## 🗑️ Полное удаление

```bash
menu
# Выбираем: 6 (удалить службу)
```

Или вручную:

```bash
systemctl stop wg-forward.service
systemctl disable wg-forward.service
rm -f /etc/systemd/system/wg-forward.service
rm -rf /opt/amneziawg-forwarder
rm -f /usr/local/bin/menu
systemctl daemon-reload
```

---

## 📄 Лицензия

Проект распространяется по лицензии **MIT**.

---

## ⭐ Поддержка проекта

Если проект оказался полезным — поставь **⭐** на GitHub!

**Ссылка на репозиторий:**  
[github.com/uristdobra/AmneziaWG-socat-forwarder](https://github.com/uristdobra/AmneziaWG-socat-forwarder)

---

## 🤝 Контрибьютинг

Предложения и исправления приветствуются! Создавайте Issues и Pull Requests.

---

**Версия:** 2.2  
**Последнее обновление:** Декабрь 2025  
**Статус:** ✅ Протестировано на Debian/Ubuntu
