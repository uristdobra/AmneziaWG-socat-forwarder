# AmneziaWG socat forwarder

Скрипт для автоматической установки и управления каскадным VPN на базе **AmneziaWG**.  
RU-сервер принимает UDP-трафик от клиента и прозрачно пересылает его на зарубежный сервер AmneziaWG через `socat`.

---

## Возможности

- ✅ Автоматическая установка пакетов (`curl`, `wget`, `socat`)
- ✅ Создание `systemd`-службы `wg-forward.service`
- ✅ Перенаправление UDP-порта с RU-сервера на зарубежный AmneziaWG-сервер
- ✅ Удобное меню управления (как в x-ui)
- ✅ Глобальная команда `menu` для управления
- ✅ Пошаговые сообщения о выполнении операций
- ✅ Автозапуск при загрузке системы

---

## Требования

- **Debian / Ubuntu** сервер (Россия)
- **Root-доступ** (`sudo`)
- Настроенный зарубежный сервер с **AmneziaWG** (self-hosted)
- Из конфигурации AmneziaWG должны быть известны:
  - **IP-адрес** сервера (из Endpoint)
  - **UDP-порт** (из Endpoint)

---

## ⚡ Быстрый старт

### Установка одной строкой через curl

```bash
 bash <(curl -fsSL https://raw.githubusercontent.com/uristdobra/AmneziaWG-socat-forwarder/main/install.sh)
```

После установки используйте команду:

```bash
menu
```

---

## 📋 Меню управления

Откройте меню командой:

```bash
menu
```

Вы увидите:

```
================ AmneziaWG socat FORWARDER ================
1. 🔧 Установить/переустановить форвардер
2. ▶️  Запустить службу
3. ⏹️  Остановить службу
4. 🔄 Перезапустить службу
5. 📊 Показать статус
6. 🗑️  Удалить службу
7. ⚙️  Установить команду 'menu'
8. ❌ Удалить команду 'menu'
0. 👋 Выход
```

Просто введите номер пункта и нажмите Enter.

---

## 🔧 Процесс установки

### Шаг 1: Установка форвардера

Выберите пункт **1** в меню.

Скрипт спросит:
- **IP зарубежного сервера** (из конфига AmneziaWG)
- **UDP-порт сервера** (из конфига AmneziaWG)

### Шаг 2: Автоматическое создание службы

Скрипт создаст и запустит `systemd`-службу `wg-forward.service`.

### Шаг 3: Обновление конфига AmneziaWG

В клиентском конфиге AmneziaWG:
- Замените IP в `Endpoint` на **IP RU-сервера**
- Оставьте **тот же UDP-порт**

Готово! Трафик теперь проходит через RU-сервер.

---

## 🎯 Как работает форвардер

```
[AmneziaWG Client] 
    ↓ UDP запрос
[RU-сервер: порт X] (socat)
    ↓ пересылка
[Зарубежный AmneziaWG-сервер: тот же порт X]
```

На RU-сервере запускается `socat`, который:
- Слушает локальный UDP-порт
- Пересылает весь трафик на зарубежный сервер
- Работает в фоновом режиме как `systemd`-служба
- Автоматически перезапускается при сбое

---

## 📖 Команды для управления

| Команда | Описание |
|---------|----------|
| `menu` | Открыть меню управления |
| `sudo systemctl status wg-forward.service` | Статус службы |
| `sudo systemctl restart wg-forward.service` | Перезапустить службу |
| `sudo systemctl stop wg-forward.service` | Остановить службу |

---

## 🏗️ Что делает install.sh

1. **Проверяет права** (требуется root)
2. **Обновляет пакеты** (`apt update && apt upgrade`)
3. **Устанавливает утилиты:** `curl`, `wget`, `socat`
4. **Спрашивает параметры:** IP и порт зарубежного сервера
5. **Создаёт файл** `/etc/systemd/system/wg-forward.service`
6. **Генерирует команду** socat с вашими параметрами:
   ```
   socat -T15 udp-recvfrom:<PORT>,reuseaddr,fork udp-sendto:<IP>:<PORT>
   ```
7. **Запускает служб** и включает автозапуск
8. **Устанавливает** глобальную команду `menu`

---

## 🔐 Безопасность

- ✅ Проверка прав доступа (требуется root)
- ✅ Валидация IP и порта
- ✅ Служба работает с ограничениями systemd
- ✅ Пересоздание сценария в `/opt/amneziawg-forwarder`

---

## ⚠️ Особенности и ограничения

- **socat перенаправляет UDP-пакеты** как простой прокси
- **Исходный IP клиента** заменяется на IP RU-сервера
- **Для больших нагрузок** убедитесь, что сервер справляется
- **fork** режим позволяет обрабатывать множество соединений
- **reuseaddr** позволяет быстро перезапустить службу

---

## 🗑️ Полное удаление

Выберите пункт **6** в меню или команду:

```bash
menu
# выберите 6 → Удалить службу
```

Это удалит:
- Службу `wg-forward.service`
- Файл конфигурации
- Скрипт из `/opt`
- **Но не удалит** команду `menu` (используйте пункт 8)

---

## 📂 Расположение файлов

| Файл | Путь |
|------|------|
| **Служба** | `/etc/systemd/system/wg-forward.service` |
| **Скрипт** | `/opt/amneziawg-forwarder/install.sh` |
| **Команда меню** | `/usr/local/bin/menu` |

---

## 🐛 Решение проблем

### Служба не запускается

```bash
sudo systemctl status wg-forward.service
```

Проверьте ошибки в выводе.

### Порт уже занят

Убедитесь, что порт не используется другим приложением:

```bash
sudo netstat -tlnup | grep <ПОРТ>
```

### Нет соединения

- Проверьте, что зарубежный сервер доступен по IP
- Убедитесь, что UDP-порт открыт в firewall
- Проверьте конфиг AmneziaWG на клиенте

---

## 📝 Примеры использования

### Пример 1: Быстрая установка

```bash
# Скачиваем и устанавливаем
curl -fsSL https://raw.githubusercontent.com/uristdobra/AmneziaWG-socat-forwarder/main/install.sh | sudo bash - установить

# Открываем меню
menu

# Выбираем: 5 → Показать статус
```

### Пример 2: Проверка статуса

```bash
sudo systemctl status wg-forward.service -l --no-pager
```

### Пример 3: Просмотр логов

```bash
sudo journalctl -u wg-forward.service -n 50 -f
```

---

## 📄 Лицензия

Проект распространяется по лицензии **MIT**.

---

## ⭐ Поддержка проекта

Если проект оказался полезным — поставь **⭐** на GitHub!

**Ссылка на репозиторий:**  
[github.com/uristdobra/AmneziaWG-socat-forwarder](https://github.com/uristdobra/AmneziaWG-socat-forwarder)

---

## 🤝 Контрибьютинг

Предложения и исправления приветствуются! Создавайте Issues и Pull Requests.

---

**Версия:** 1.0  
**Последнее обновление:** Декабрь 2025
